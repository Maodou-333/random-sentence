# 随机造句

一个使用原生 HTML、CSS 和 JavaScript 制作的随机句子生成网站，可以直接部署到 GitHub Pages。

## 本地打开

直接双击 `index.html` 即可运行，无需安装任何软件或依赖。

## 文件说明

- `index.html`：网页结构
- `style.css`：页面样式
- `script.js`：随机生成、锁定与复制功能
*本网站借助于ai生成
